# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode
.idea
*.swp
*.swo
*~

# typescript incremental build cache
*.tsbuildinfo

# editor / tooling (matches the existing .vscode rule)
.claude

# internal npm proxy — machine-specific, breaks CI that cannot reach it
.npmrc

# High-resolution image sources. The served AVIFs in public/ are generated from
# these (see scripts/optimise-images.mjs); the 2 MB PNGs are kept locally only.
/assets-originals
